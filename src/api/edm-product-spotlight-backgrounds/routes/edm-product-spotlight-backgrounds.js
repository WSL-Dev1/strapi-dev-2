'use strict';

/**
 * edm-product-spotlight-backgrounds router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::edm-product-spotlight-backgrounds.edm-product-spotlight-backgrounds');
