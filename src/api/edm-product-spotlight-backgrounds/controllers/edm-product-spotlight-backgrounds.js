'use strict';

/**
 * edm-product-spotlight-backgrounds controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::edm-product-spotlight-backgrounds.edm-product-spotlight-backgrounds');
