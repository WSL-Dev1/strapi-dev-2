'use strict';

/**
 * edm-product-spotlight-backgrounds service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::edm-product-spotlight-backgrounds.edm-product-spotlight-backgrounds');
