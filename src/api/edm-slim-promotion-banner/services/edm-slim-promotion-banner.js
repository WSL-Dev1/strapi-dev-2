'use strict';

/**
 * edm-slim-promotion-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::edm-slim-promotion-banner.edm-slim-promotion-banner');
