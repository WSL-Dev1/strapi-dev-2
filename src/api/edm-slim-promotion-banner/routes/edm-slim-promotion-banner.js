'use strict';

/**
 * edm-slim-promotion-banner router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::edm-slim-promotion-banner.edm-slim-promotion-banner');
