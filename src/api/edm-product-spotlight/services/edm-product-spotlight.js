'use strict';

/**
 * edm-product-spotlight service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::edm-product-spotlight.edm-product-spotlight');
