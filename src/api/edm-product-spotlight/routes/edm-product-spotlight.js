'use strict';

/**
 * edm-product-spotlight router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::edm-product-spotlight.edm-product-spotlight');
