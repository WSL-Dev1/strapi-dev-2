'use strict';

/**
 * sex-survey-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sex-survey-page.sex-survey-page');
